# What does #include do?

- This is a directive to the preprocessor that runs when you call your compiler.
This specifc directive causes the contents of the file named in <> after #include to be inserted at the line as
    if it were typed at that location in your source code.